{% extends 'base.html.twig' %}

{% block body %}
    <h1>Factureordonance</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ factureOrdonance.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('factureordonance_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('factureordonance_edit', { 'id': factureOrdonance.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
