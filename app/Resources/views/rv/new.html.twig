{% extends 'base.html.twig' %}

{% block body %}

    <div class="row mb-2">
        <div class="col-sm-6">
            <h1 class="x_title">Rendez-Vous Edit</h1>
        </div>
        <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
                <li class="breadcrumb-item"><a href="{{ path('rv_index') }}">Rendez-Vous</a></li>
                <li class="breadcrumb-item active">Edit</li>
            </ol>
        </div>
    </div>
{#
    <form action="{{ path('rv_new') }}" method="post" class="form-horizontal form-label-left">

        {{ form_errors(form) }}

        <div class="form-group">
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_label(form.dateRendezVous, "date RendezVous") }}
            </div>
            {{ form_errors(form.dateRendezVous) }}
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_widget(form.dateRendezVous) }}
            </div></div>
        <div class="form-group">
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_label(form.medecin, "medecin") }}</div>

            {{ form_errors(form.medecin) }}
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_widget(form.medecin) }}
            </div>
        </div>
        <div class="form-group">
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_label(form.patient, "patient") }}</div>

            {{ form_errors(form.patient) }}
            <div class="control-label col-md-3 col-sm-3 col-xs-12">
                {{ form_widget(form.patient) }}
            </div>
        </div>

        <div class="ln_solid"></div>
        <div class="form-group">
            <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                <button class="btn btn-primary" type="button">Cancel</button>
                <button type="submit" class="btn btn-success">Submit</button>
            </div>
        </div>
        {{ form_rest(form) }}

    </form>#}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-success"/>
    {{ form_end(form) }}
    <div class="row m-md-2">
        <a class="btn btn-dark" href="{{ path('rv_index') }}"><i class="fa fa-arrow-left"></i>Back to the list</a>
    </div>
{% endblock %}
